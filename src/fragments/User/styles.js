import { makeStyles } from '@material-ui/core';

const userStyles = makeStyles({
  submitButton: {
    marginRight: '10px',
  },
  deleteButton: {
    marginRight: '10px',
  },
  updateButton: {
    marginRight: '10px',
  },
});

export default userStyles;
