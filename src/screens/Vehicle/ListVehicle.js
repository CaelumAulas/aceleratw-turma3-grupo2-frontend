const { GridFullHeight } = require("components/GridFullHeight/GridFullHeight");

const ListVehicle = () =>{

    return(
        <GridFullHeight>
        
        </GridFullHeight>
    );
}
export default ListVehicle;