import React from "react";
import { ContainerFullHeight } from "components/ContainerFullHeight/ContainerFullHeight";
import LoginForm from "fragments/Login/LoginForm/LoginForm";

const Login = () => {
  return (
    <ContainerFullHeight>
      <LoginForm />
    </ContainerFullHeight>
  );
};

export default Login;
